import type { SxProps } from "@mui/material";

const style: SxProps = {
    "& .header": {
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        marginBottom: "24px",
    }
}

export default style;